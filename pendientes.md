# Pendientes

Marcar `[x]` cuando quede resuelto y verificado.

Orden basado en dependencias reales entre módulos (Sistema → Inventario → Compras → Ventas)
y en qué ítems bloquean o son prerrequisito de otros.

## General

- [x] Al crear empresa, en Documentos no hay secuencias y da error (debe crear secuencia automática).
- [x] En barra principal superior, al lado del usuario/nombre, agregar qué bodega está activa: "BODEGA1 - Administrador".

## Empresa

- [x] Agregar dos campos que afectan facturación y nota de venta: % descuento máximo facturas, % descuento máximo notas de venta.
- [x] Verificar que al dar todos los permisos a un usuario con perfil GERENTE, no le deje editar su propia empresa. (No era bug de código: faltaba otorgar el permiso "Editar empresa" al perfil desde Sistema → Perfiles. Ya asignado y confirmado en BD)
- [x] Nueva factura: en Empresa agregar opción para vender solo lo de su propia bodega o de todas (predeterminado solo su propia bodega). (Se implementó por Perfil en vez de por Empresa: switch "Vender stock de todas las bodegas" en Sistema → Perfiles, ya que permite distinguir GERENTE (ve todas) de VENDEDOR (solo su bodega) dentro de la misma empresa. Aplica a Nueva factura/Nota de venta; Proforma no fue tocada)

## Producto

- [x] En /Nuevo producto: al crear otro usuario y darle permisos por perfil, no se ve el "+" y no permite crear categoría y marca. (Causa: los menús padre "Categorias"/"Marcas" estaban inactivos, lo que ocultaba "Crear categoria"/"Crear marca" del árbol de permisos de Perfiles. Se reparentaron bajo "Productos" — sin crear menús propios — y ya se pueden otorgar por perfil)
- [x] Al importar producto dice "omitido" y da el detalle al hacer clic; agregar por qué está mal el detalle. Ej. precio: 45as4,00. (StockControlador::validarCampoNumerico() ahora distingue "valor no numérico" de "usa punto decimal" y muestra el valor recibido en el mensaje)

## Ajustes

- [x] Al buscar y escoger producto: hay un "visto" (esto no va como el resto del sistema), pasar a dar clic en toda la fila. (Modal "Buscar producto" de Ajustes/Movimientos ahora usa fila completa clickeable, igual que Facturación)
- [x] Al existir una sola bodega, debe elegirla predeterminadamente. (Solo aplicado a Ajuste, no a Transferencia — ver nota en Movimientos Internos)
- [x] El modal de detalles de ajuste es diferente a lo que va con el resto del sistema. (Revisado: el modal de Ajuste/Movimiento ya usa el patrón `doc-*`, formal y con badges de color — es de los 3 el mejor estructurado. El problema real es que Compras y Facturas/Proformas usan cada uno su propio estilo distinto entre sí. Ver nota en "Deuda técnica" al final)
- [x] En Ajuste → Nuevo ajuste, ocultar el campo Bodega origen. (La cabecera ya lo tenía oculto; el problema real era la columna "Origen" por línea en la tabla, que siempre se veía deshabilitada junto a "Destino" según la Acción. Se fusionaron ambas en una sola columna "Bodega" por línea — muestra el selector de Origen o de Destino según si la Acción es Egreso o Ingreso, alternando visibilidad en vez de solo deshabilitar. Transferencia no cambia, sigue usando los combos del encabezado)
- [ ] En Ajuste → Nuevo ajuste, al guardar preguntar "¿Imprimir?" y si la respuesta es sí, abrir el PDF. (SALTADO por decisión del usuario: no siempre es posible imprimir directo al guardar porque el movimiento puede requerir aprobación primero — si no queda autoaprobado, no hay documento procesado/con stock aplicado todavía para imprimir. Revisar más adelante si aplica solo al caso autoaprobado)

## Kardex

- [ ] En detalles del movimiento hay documentos que dejan hora = 00:00. (EN ESPERA: falta que el usuario especifique de qué tipo de documento se trata)
- [x] En detalles del movimiento no se pudo generar PDF de ajuste (debería ser una llamada a una función común). (No era Ajuste sino Factura/Nota de venta: `abrirPdfInventario()` en sistema.js comparaba contra 'FACTURA_VENTA'/'NOTA_VENTA' pero el kardex guarda 'VEN_FACTURA' — el string nunca coincidía y caía al endpoint equivocado. Corregido para que compare contra el valor real)
- [x] Si solo hay una bodega, seleccionarla automáticamente. (No aplica: Kardex no tiene selector de bodega — las bodegas se muestran como columnas paralelas, no como un combo a elegir. Era un duplicado del ítem ya resuelto en Ajustes)
- [x] En el detalle de movimientos, agregar qué usuario realizó el movimiento. (Nueva columna "Usuario" al final de la tabla — se agregó `usuario_nombre` con LEFT JOIN a sis_usuarios por `usuario_crea` en `KardexModelo::listarDetalleProducto()`. No afecta el PDF ni la consulta principal)

## Movimientos Internos

- [x] Nueva transferencia: mover/quitar origen y destino por línea y que sea en encabezado por todo el documento. (Origen/Destino ahora son 2 combos en el encabezado que aplican a todas las líneas; se sincronizan automáticamente y validan que no sean la misma bodega. La columna "Accion" y el resto de columnas de la tabla no se tocaron. Ajuste queda sin cambios)
- [x] Nueva transferencia: moviendo origen/destino al encabezado, en modal "Buscar Producto" cambiar esas bodegas a columnas horizontales y quitar la fila de stock vertical. Ejemplo: si en encabezado escogí origen=Matriz, destino=Bodega1, en modal "Buscar Producto" las columnas de stock serían: MATRIZ | BODEGA1. (Se bloquea la búsqueda con aviso si falta elegir Origen/Destino. Ajuste no cambia, sigue con el formato apilado de todas las bodegas)
- [x] En consulta: el filtro debe ser por DESTINO = mis bodegas activas. (Agregado botón "Filtrar". Corregido en el camino: el destino de Ajuste vive por línea en inv_movimientos_detalle, no en el encabezado — filtrar por encabezado hubiera ocultado todos los ajustes)
- [x] PDF de transferencia: el detalle debe ser horizontal, no vertical por movimiento. Columnas: CODIGO | PRODUCTO | ACCION (+/-) | CANTIDAD | SALDO. (Solo aplicado a Transferencia; Ajuste conserva su formato con columna Bodega)
- [x] PDF de transferencia: en el encabezado agregar Bodega Origen y Bodega Destino. (Implementado junto con el anterior)

## Compras

- [x] Intercambiar columnas de Nueva compra así: Cod. Interno | Cod. Proveedor.
- [x] En Nueva compra, agregar columna "Marca" -> tipo combo. (Editable: se guarda como historial en com_documento_detalle_marca_id, no actualiza el producto global. La actualización global quedó comentada en DocumentoCompraModelo::crear()/actualizar() por si se necesita más adelante)
- [x] Modal "Crear producto" si en búsqueda producto en Nueva compra, igual que en "Ventas/Facturación/Nueva factura". (Reutiliza el endpoint existente /inventario/productos/crear en modo AJAX. Bug propio corregido en el camino: el campo empresa_id del formulario solo existe para superusuario, causaba error "q(...) is null" al guardar para usuarios normales)
- [x] En Nueva compra, en campo "Cod. Interno" asignar secuencia automáticamente. Ej. si el último código fue 1000-A105, siguiente 1001-A106. (Implementado junto con el modal "Crear producto": `ProductoModelo::sugerirSiguienteCodigo()` mantiene el prefijo de letras y sube el número; se recalcula localmente en JS tras cada producto creado en el mismo documento)
- [x] En Nueva compra, mover descuento del detalle al total (como ya está) pero con un input modificable que actualice el precio en vivo. (Se quitó la columna "Desc." por línea; el detalle ahora calcula su total solo con Cant.×Costo+IVA. En Totales, "Descuento" es un input en % (corregido desde una versión inicial en $ que estaba mal) que aplica fórmula fija C: reduce solo la base, el IVA se suma completo sin descuento. Sin tope configurado. El % se convierte a $ en un input hidden antes de enviar el form nativo. No se tocó BD: se sigue guardando en com_documento_descuento de cabecera, líneas quedan con descuento 0)
- [x] En Nueva compra, cuando el cursor esté en PVP (último campo), agregar nueva línea automáticamente con Enter. (Enter en PVP agrega la fila y mueve el foco a Cod. Interno de la nueva línea; en el resto de campos Enter sigue sin hacer nada, salvo Cod. Interno/Cod. Proveedor que ya abrían el modal de búsqueda)
- [x] En Nueva compra, en cabecera el campo Bodega destino: quitar el código de bodega, dejar solo el nombre.

## Facturar

- [x] Nueva factura: en modal "Buscar Cliente" debe salir el resultado aunque el input de búsqueda esté vacío. (Se quitó el corte de "mínimo 2 caracteres" en FacturaControlador::buscarClientes(); con término vacío ahora trae hasta 30 clientes activos ordenados por razón social, igual que ya hacía Buscar Producto)
- [x] Nueva factura: en modal "Buscar Cliente" el botón "Crear Cliente" debe ocultarse al estar creando el cliente y mostrarse nuevamente al dar Enter en el input "Buscar". (El botón se oculta en expandirFormularioNuevoCliente(); Enter en el buscador cancela el debounce y relanza la búsqueda de inmediato, lo que cierra el panel y regenera el botón si sigue sin coincidencias)
- [x] Nueva factura: en encabezado debe verse correo y teléfono del cliente. (2 campos solo lectura en Fila 2, junto a Forma de pago; se llenan automático por RUC+Enter, desde el modal Buscar Cliente y al crear cliente rápido — las 3 rutas ya devolvían esos datos, solo faltaba mostrarlos)
- [x] Nueva factura: forma de pago (agregar botón "+" al lado del combo para abrir un mini CRUD sencillo de crear, editar, eliminar, similar a Marca y Categoría de producto). (Botón "+" abre modal con lista + form crear/editar/activar-inactivar/eliminar. Permisos granulares nuevos `/ventas/formas-pago/{crear,editar,inactivar,eliminar}` como hijos de `/ventas/facturas` — sin entrada propia en el menú lateral, igual que Códigos de Proveedor. Eliminar se bloquea si la forma de pago está en uso en documentos existentes, solo permite inactivar. Nuevo FormaPagoModelo + métodos en FacturaControlador, requiere `require_once` manual en index.php ya que el proyecto no usa autoload PSR-4)
- [x] Nueva factura: forma de pago debe crear predeterminados: EFECTIVO, TRANSFERENCIA, TARJETA CRÉDITO. (Se agrega en EmpresaModelo::crear(), junto a perfiles y secuencias base, cada vez que se crea una empresa nueva. Con código SRI ya asignado (01/16/19) y solo EFECTIVO con calculadora de cambio. Bug real encontrado y corregido en el camino: la consulta SQL fallaba con "Ambiguous parameter" en PostgreSQL por falta de CAST explícito, mismo problema ya visto antes en PerfilModelo::crearPerfilBase())
- [x] Nueva factura: mover descuento del detalle al total con input editable. (Se quitó la columna "Desc%" por línea; el detalle ahora calcula su total solo con Cant.×Precio+IVA. En Totales, "Descuento" es un input en % (corregido desde una versión inicial en $ que estaba mal) con 3 fórmulas seleccionables por empresa —A: reduce base antes de IVA, B: reduce el total con IVA incluido, C: reduce solo la base y el IVA se suma completo, predeterminado—, configurable en Sistema → Empresas → Facturación (`sis_empresa_formula_descuento`). Valida contra el % máximo ya existente por tipo de documento (factura/nota de venta), tanto en JS como en el backend (`FacturaControlador::validarDescuentoMaximo()`). No afecta el flujo de aprobación de precio. No se tocó el esquema de persistencia: sigue guardando $ calculado en ven_documento_descuento)
- [x] Nueva factura: quitar IVA del detalle (ya está en el total). (DECISIÓN: no se oculta la columna. Se descubrió que el select de IVA por línea es hoy la única forma de marcar un producto exento —no hay automatización por producto (`inv_producto_lleva_iva`) en la búsqueda—, así que ocultarla sería una regresión funcional, no solo estética. En su lugar se corrigió un bug real encontrado en el camino: `listarIva()` en Factura/Proforma/Compra ordenaba por valor ascendente y tomaba la primera fila como "predeterminada", lo que hacía que 0% ganara sobre 15% cuando ambas tasas estaban activas. Se agregó `sis_iva_predeterminado` (migración `20260730_1100_sis_iva_predeterminado.sql`, con backfill a la tasa de mayor valor) y se corrigió el ORDER BY en los 3 modelos. No existe pantalla para gestionar tasas de IVA — sigue siendo por SQL, decisión explícita de no construir una ahora. Automatización de IVA por producto queda pendiente, no se abordó)
- [x] Consulta del PDF: quitar IVA del detalle y dejarlo solo en el total. (DECISIÓN: se salta, misma lógica que el ítem anterior. LA LÓGICA ESTÁ BIEN PORQUE ES IVA POR PRODUCTO y Total de ese IVA — el detalle muestra el IVA de cada línea porque es por producto, y el Total ya suma ese IVA; no es redundante, son dos niveles de información distintos)
- [x] Formas de pago: convertir en una tabla contra la venta, con campos propios por tipo (aplica a Factura y Nota de Venta). (Opción 1: combo de Forma de pago + campos condicionales en la misma fila — Nombre/No.Comprobante para Tarjeta Crédito/Transferencia/Depósito/DeUna, Días para Crédito — y "Mixto" como opción nueva que despliega checklist + tabla con Monto por forma de pago marcada, validando que la suma sea igual al Total. Crédito es EXCLUSIVO, no combinable en Mixto (decisión del usuario, ampliable a futuro). Nuevo campo `ven_forma_pago_tipo` en el catálogo de Formas de Pago (con select "Tipo" agregado al mini-CRUD) y tabla nueva `ven_documento_forma_pago` para el detalle. Aprovechado para reusar `ven_documento_plazo_pago_dias` (ya existía, alimenta el XML del SRI) y para limpiar `generarCxcSiAplica()` — ahora genera CXC solo cuando la forma de pago principal es tipo CREDITO, en vez de comparar por nombre de texto. LIMITACIÓN CONOCIDA: al editar una factura ya creada, no se restaura el detalle de Mixto guardado (nombre/comprobante/monto por fila) — el combo vuelve a mostrar solo la forma de pago principal. No se implementó por alcance/tiempo, pendiente si se necesita)
- [x] Si el usuario tiene más de una bodega asignada, mostrar un selector de bodega en Nueva Venta (Factura/Nota de Venta). (Nuevo `FacturaModelo::listarBodegasPermitidas()`; el combo "Bodega" solo aparece en la Fila 1 si hay 2+ bodegas asignadas, si no se usa la predeterminada como antes, sin cambios visuales. La bodega elegida se valida en el backend contra las permitidas del usuario antes de usarla — no se confía en lo que envíe el navegador. También se propagó al buscador de productos (`/ventas/facturas/productos`) para que el Stock mostrado en la búsqueda corresponda a la bodega elegida, no siempre a la predeterminada. Solo para usuarios normales; superusuario no ve el selector, mismo comportamiento que ya tenía)
- [x] En el campo Cantidad, mostrar el stock de mi bodega (mismo patrón del ícono de candado que ya existe junto a Código). (Se quitó la columna "Stock" separada; ahora es un badge pegado al input de Cantidad, mismo input-group que el candado de Código, mostrando el stock entero (sin decimales). Es clicable: abre un modal nuevo "Stock por bodega" con el desglose horizontal por cada bodega activa de la empresa para ese producto — nuevo endpoint `/ventas/facturas/stock-bodegas` + `FacturaModelo::listarStockPorBodega()`)
- [x] Dar Enter en el campo Precio agrega una línea nueva automáticamente (mismo patrón ya implementado en Compras con PVP). Aplica a Factura y Nota de Venta. (Enter en Precio agrega la fila y mueve el foco a Codigo de la nueva línea)
- [x] Cantidad a 2 decimales; las flechas de aumentar/disminuir deben moverse de a unidades enteras, no en decimales. (`step="1"` para que las flechas suban/bajen de 1 en 1, `min="0.01"` sigue permitiendo escribir decimales a mano; se redondea a 2 decimales tanto en el payload del JS como en el backend `normalizarLineas()`)
- [x] Al buscar producto en Nueva factura/Nueva nota de venta, mostrar 2 columnas de stock: STOCK MI BODEGA | STOCK TODAS LAS BODEGAS. (`FacturaModelo::buscarProductos()` ahora calcula ambos agregados en una sola consulta — `stock_mi_bodega` vía `SUM(CASE WHEN inv_bodega_id = :bodega_id ...)`, `stock_total` sin filtrar. Para superusuario/venta_todas_bodegas, mi_bodega = total ya que no aplica el concepto. El badge de stock junto a Cantidad también se actualizó para usar `stock_mi_bodega` en vez del total)
- [x] Bug: al facturar, el PDF generado dice "Proforma" en lugar de "Factura". (Causa real: como aún no existe autorización SRI, toda Factura/Nota de Venta queda en estado CREADA, y `FacturaControlador::pdf()` solo tiene 2 casos — AUTORIZADA usa `generarFacturaAutorizada()`, cualquier otro estado cae a `generarProforma()`, que tenía el título "PROFORMA" fijo en el HTML. Se agregó `$tituloDoc` dinámico en `GeneradorPdf::crearHtmlProforma()` según `tipo_codigo` (FACTURA_VENTA→"FACTURA", NOTA_VENTA→"NOTA DE VENTA", default→"PROFORMA") — no afecta el PDF real de Proformas, que sigue mostrando "PROFORMA" igual que antes)
- [ ] En consulta de facturas, agregar opción de enviar al SRI por lote. (Depende del módulo de Autorización SRI, que aún no está iniciado — ver `project_status.md`)

## Proformas

- [x] En el PDF de Proforma, si el stock del producto es 0, colocar un asterisco al inicio de la fila. (Se agregó `stock_total` a `ProformaModelo::listarDetalle()` (subquery a `inv_stock`, sumado en todas las bodegas) y una leyenda "* Sin stock disponible" debajo de la tabla si aplica a alguna línea. Como `GeneradorPdf::crearHtmlProforma()` es compartido con el PDF de Factura/Nota de Venta (bug de la tarjeta anterior), el chequeo también reconoce el campo `stock_disponible` que usa esa ruta, para no marcar falsos positivos ahí)
- [x] Bug: en Nueva proforma, el buscador de clientes no carga resultados. (Mismo bug ya resuelto antes en Facturar pero nunca aplicado a Proforma: `ProformaControlador::buscarClientes()` cortaba con "mínimo 2 caracteres" — al abrir el modal sin escribir nada, la búsqueda salía vacía. Se quitó el corte, igual que en Factura; con término vacío ya trae hasta 30 clientes activos)
- [x] Quitar el campo Forma de pago del formulario de Nueva proforma (la Proforma es solo una cotización, no requiere registrar forma de pago). (Se quitó el select, la validación y `formasPago`/`listarFormasPago()` que solo servían para esto. `ven_forma_pago_id` ahora se guarda NULL al crear una proforma. Al convertir una Proforma en Factura ya existía un fallback a EFECTIVO si no había forma de pago — ahora ese fallback aplica siempre. NOTA: esa conversión no pasa por el nuevo mecanismo de `formas_pago[]`/Mixto de Facturar — sigue usando el flujo simple de un solo `forma_pago_id`, no se tocó por estar fuera de este pendiente)

## Clientes

- [x] En acciones, agregar ícono "Historial de ventas": modal para ver listado de facturas, notas de venta y proformas. (Modal con 3 pestañas — Facturas, Notas de Venta, Proformas —, cada una un DataTable real con buscador y paginación propios. Filtro Desde (día 1 del mes actual)/Hasta (hoy) + botón "Filtrar" arriba de las pestañas, aplica a las 3 tablas a la vez (mismo patrón `ext.search` ya usado en Movimientos Internos). Columna "#" al inicio numera las filas visibles y su encabezado muestra el total de registros filtrados (no dice "#"); "Total filtrado" arriba de cada tabla suma la columna Total — ambos se recalculan en cada `draw` del DataTable, sobre filas en orden filtrado/ordenado real. Cada fila tiene "Ver PDF" reutilizando las rutas ya existentes. Nuevo endpoint `/ventas/clientes/historial`, nuevos `listarPorCliente()` en FacturaModelo y ProformaModelo. Bug real encontrado y corregido en el camino: el script inline usaba jQuery/DataTables pero se ejecutaba antes de que esas librerías cargaran (se cargan al final, en plantillas/pie.php) — se solucionó envolviendo el script en `DOMContentLoaded`)

## Anulación de documentos (transversal a todos los módulos)

- [x] Al anular cualquier documento (Compras, Ventas: Factura/Nota de Venta/Proforma, Movimientos Internos: Ajuste/Transferencia), exigir un campo "Motivo". (Columnas nuevas `ven_documento_motivo_anulacion`/`com_documento_motivo_anulacion`/`inv_movimientos_motivo_anulacion`. En los 4 módulos, el diálogo de confirmación de anular ahora pide el motivo con un input de SweetAlert, obligatorio, validado también en el backend. Para el patrón genérico `.formulario-confirmar` de sistema.js se agregó el atributo opcional `data-pedir-motivo="1"` — no afecta a los demás usos de ese patrón que no lo tengan)
- [x] Al anular, agregar una marca de agua "ANULADA" en el PDF correspondiente. (Nueva función `GeneradorPdf::marcaAguaAnulado()` reutilizada en Factura/Nota de Venta/Proforma y en Movimientos Internos; Compras usa su propio generador en `PdfSri/Plantillas.php`, se le agregó la misma lógica ahí. Todos condicionan por `sis_estado_codigo === 'ANULADA'/'ANULADO'`)
- [x] En "Ver detalle", si el documento está anulado, mostrar "ANULADA" en rojo. (Se agregaron clases CSS `.estado-anulada`/`.estado-anulado` en rojo — antes no existían y el badge salía sin color. Se agregó también el Motivo capturado, en rojo, junto al estado, en los 4 módulos)

## Deuda técnica (no urgente, hacer cuando haya tiempo)

- [ ] Normalizar los modales "ver detalle" de documentos al patrón `doc-*` que ya usa Ajuste/Movimiento (header con badge de estado, tabla de campos, thead estilizado):
  - [ ] Compras → Ver documento (`documentos.php`): hoy usa inputs deshabilitados en grilla; mantener el bloque de Subtotal/Descuento/IVA/Total que es propio de Compras.
  - [ ] Facturas → Ver factura (`facturas.php`): hoy usa tablas clave-valor de 2 columnas sin badge de color.
  - [ ] Proformas → Ver proforma (`proformas.php`): el que menos estructura tiene hoy; mismo tratamiento que Facturas.
  - Kardex y Stock quedan fuera de esto — sus modales no son "detalle de documento" (son listas/desgloses de otra naturaleza).
